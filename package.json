{
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2"
  },
  "name": "my-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build-types": "npm --prefix backend run prisma:generate  && npm --prefix backend run prisma:migrate"
  },
  "dependencies": {}
}
